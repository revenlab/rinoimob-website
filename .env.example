# Client-side API URL — empty = uses Nuxt proxy (no CORS issues)
# Set to full URL (e.g. https://api.example.com) in production if not using proxy
NUXT_PUBLIC_API_URL=

# Internal backend URL used by Nuxt server-side proxy
NUXT_API_INTERNAL_URL=http://localhost:39000
